# Lily's VIM Configuration

Ideally I'd give you a command to clone directly into your _home directory,_ but
I'm being lazy, so you must put these two items there yourself:

  * `.vimrc`
  * `.vim/`

You can press `\` then `c` to enable/disable spellcheck in VIM with this
configuration.
